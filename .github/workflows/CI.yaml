name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: Setup project
        run: make setup
      - name: Linter's check up
        run: make lint
      - name: Test's check up
        run: make test
